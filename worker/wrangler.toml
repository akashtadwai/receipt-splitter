name = "receipt-splitter-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# The worker URL will be: https://receipt-splitter-api.<your-subdomain>.workers.dev
# You can also add a custom domain later

[vars]
# Non-sensitive environment variables can go here
ALLOWED_ORIGIN = "https://akashtadwai.github.io"

# Secrets (like MISTRAL_API_KEY) should be set via:
# wrangler secret put MISTRAL_API_KEY
# or via GitHub Actions using Cloudflare secrets
